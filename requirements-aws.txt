    # Minimal runtime dependencies for AWS Lambda zip
# Keep this list lean; transitive deps will be resolved automatically
fastapi==0.121.0
mangum>=0.17.0,<0.18.0
pydantic-settings>=2.0.0
openai>=1.0.0
# crewai is t   oo large for direct deployment - TODO: use Lambda Layer or Container Image
mangum